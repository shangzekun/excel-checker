services:
  excel-checker:
    build: .
    container_name: excel-checker
    ports:
      - "8000:8000"
    environment:
      ENV: prod
      MAX_MB: 10
      LOG_LEVEL: INFO
      ALLOW_ORIGINS: http://127.0.0.1:8000,http://localhost:8000
    restart: unless-stopped
